storage 数据支持
=============


```js
    var db = require('storage');

    db.use('aoPackage'); // 使用aoPackage作为数据集
    db.set('message', 'ok');
    var msg = db.get('message');

    
    db.get()
```

-- storage 还在完善中，敬请期待。。。